# This file is automatically generated by ravepipeline::mcptool_build(). 
# Please never edit this file by hand!
name: ieegio-mcp_tool_volume_clear
description: Remove one or all volumes from the session cache. This frees up memory
  occupied by cached volume objects. If an ID is provided, only that specific volume
  is removed. If no ID is provided, all cached volumes are cleared.
parameters:
  type: object
  properties:
    id:
      type: string
      description: 'The digest-based identifier of the volume to remove. Empty string
        (default) clears all volumes. Example values: "a3f5e8d9c1b2", "f7e4b2a1c6d8"'
      default: ''
      examples:
      - a3f5e8d9c1b2
      - f7e4b2a1c6d8
category: execution
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    cleared_ids:
      type: array
      description: Character vector, IDs of volumes that were removed
      items:
        type: string
    message:
      type: string
      description: Character, human-readable status message
    success:
      type: boolean
      description: Logical, whether the operation completed successfully
implementation_example: |
  # Clear specific volume from cache
  # Note: .state_env is automatically provided when called via MCP.
  # AI does not need to specify this argument. The examples only use
  # .state_env to make the code runnable for demonstration purposes.
  state_env <- fastmap::fastmap()
  sample_file <- ieegio_sample_data("nifti/rnifti_example.nii.gz")
  read_result <- ieegio:::mcp_tool_volume_read(sample_file, .state_env = state_env)
  # Clear the specific volume
  ieegio:::mcp_tool_volume_clear(
    id = read_result$id,
    .state_env = state_env
  )
  #
  #> $success
  #> [1] TRUE
  #> 
  #> $message
  #> [1] "Cleared 1 volume from cache"
  #> 
  #> $cleared_ids
  #> [1] "e1ccd803b62bb9390d3debbd05f32042"
  #>
execution_time: 0.016
